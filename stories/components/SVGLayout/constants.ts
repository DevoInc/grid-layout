import type { TProps } from './declarations';

export const PADDING = [5, 1];
export const SCALE = 10;

export const PROPS: TProps[] = ['x', 'y', 'w', 'h'];
export const POS_PROPS: TProps[] = ['x', 'y'];
export const SIZE_PROPS: TProps[] = ['w', 'h'];
export const HOR_PROPS: TProps[] = ['x', 'w'];
