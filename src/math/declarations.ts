export type TPos = { x: number; y: number };
export type TRect = TPos & { w: number; h: number };
export type TInterval = [number, number];
export type TAABB = { x1: number; y1: number; x2: number; y2: number };
