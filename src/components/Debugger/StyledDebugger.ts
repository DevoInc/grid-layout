import styled from 'styled-components';

export const StyledDebugger = styled.div`
  position: fixed;
  top: 0;
  z-index: 100;
  pointer-events: none;
  width: 100%;
  height: 100%;
  overflow: hidden;
`;
